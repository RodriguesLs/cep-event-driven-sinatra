# frozen_string_literal: true

RSpec.describe Cep::Event::Driven do
  it 'has a version number' do
    expect(Cep::Event::Driven::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
